defaults:
  - data: default
  - model: default
  - _self_

train:
  max_epochs: 30
  batch_size: 32
  gradient_accumulation_steps: 4
  accelerator: "gpu"
  devices: 2                # number of GPUs
  strategy: "ddp"          
  precision: 16-mixed
  log_every_n_steps: 50
  enable_progress_bar: true
  enable_model_summary: true

wandb:
  project: cxr-lung-disease-classification 
  name: null
  entity: mustaphamensouri
  notes: "baseline run"             
  tags:
    - baseline
    - Vit
    - google/vit-large-patch16-384